version: 0.0
os: linux
files:
  - source: target/devops-practice-0.0.1-SNAPSHOT.jar
    destination: /home/ec2-user
hooks:
  ApplicationStart:
    - location: scripts/run.sh
      timeout: 300
      runas: ec2-user
